import React, {ReactElement, FC, useEffect, useState} from "react";
import {useNavigate} from "react-router-dom";

const Question: FC<any> = (props): ReactElement => {
    const navigate = useNavigate();

  return ( 
    <div></div>
  )
};

export default Question;